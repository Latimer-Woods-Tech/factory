{
  "name": "@adrper79-dot/prime-self-smoke",
  "version": "0.1.0",
  "private": true,
  "description": "Production Playwright smoke tests for selfprime.net",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:workspace-contract": "playwright test tests/workspace-contract.spec.ts"
  },
  "dependencies": {
    "@adrper79-dot/testing": "file:../../packages/testing"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.2",
    "@playwright/test": "^1.44.0"
  }
}
