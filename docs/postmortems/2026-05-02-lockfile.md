# 2026-05-02: Dependabot batch broke admin-studio deploy

**Severity:** P2  **Duration:** ~1.5 hours  **Impact:** admin-studio deploy blocked

## What happened
Batch-merged 5 Dependabot PRs. Three compounding failures:
1. `package-lock.json` desync → `npm ci` EUSAGE failures
2. `workers-types` v4 renamed `ScheduledEvent` → `ScheduledController` → TypeScript errors
3. `@latimer-woods-tech/llm@0.3.0` removed `stream` export → more TS errors

## Root cause
Batch merges don't update lockfiles atomically. `@cloudflare/workers-types` had breaking renames in a non-major bump.

## What we changed
- `deploy-admin-studio.yml` uses `npm install --legacy-peer-deps` instead of `npm ci`
- Added batch-merge warning to `dependency-management.md` playbook
