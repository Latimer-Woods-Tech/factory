# docs/archive

Historical planning and completion documents moved here as part of VK-8 root cleanup.
These files are preserved for reference but are no longer active.

All active documentation lives under `docs/` and the repository root `README.md` / `CLAUDE.md`.
