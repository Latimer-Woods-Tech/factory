---
title: Introduction
description: Factory Core is the shared infrastructure layer for all Factory applications.
---

# Factory Core

Factory Core is a collection of 19 TypeScript packages that provide battle-tested, reusable infrastructure for Cloudflare Workers applications. Every Factory app installs only what it needs.

## Stack

| Layer | Technology |
|---|---|
| Runtime | Cloudflare Workers |
| Router | Hono |
| Database | Neon Postgres via Hyperdrive |
| Auth | JWT / Web Crypto API |
| LLM | Anthropic → Grok → Groq failover |
| Telephony | Telnyx + Deepgram + ElevenLabs |
| Email | Resend |
| Errors | Sentry |
| Analytics | PostHog + `factory_events` |

## Packages

| Stage | Package | Purpose |
|---|---|---|
| 1 | `@latimer-woods-tech/errors` | Typed error hierarchy, `FactoryError`, `toErrorResponse` |
| 1 | `@latimer-woods-tech/monitoring` | Sentry integration for Cloudflare Workers |
| 1 | `@latimer-woods-tech/logger` | Structured JSON logger with levels and context |
| 1 | `@latimer-woods-tech/auth` | JWT issue/verify/refresh, `jwtMiddleware`, `requireRole` |
| 1 | `@latimer-woods-tech/neon` | Neon Postgres client via Hyperdrive, `withDbRetry` |
| 1 | `@latimer-woods-tech/stripe` | Stripe webhook handling, subscription lifecycle |
| 2 | `@latimer-woods-tech/llm` | Anthropic → Grok → Groq failover, streaming, `withSystem` |
| 2 | `@latimer-woods-tech/telephony` | Deepgram STT, ElevenLabs TTS, Telnyx webhooks, `VoiceSession` |
| 3 | `@latimer-woods-tech/analytics` | PostHog + `factory_events` DB table, event tracking |
| 3 | `@latimer-woods-tech/deploy` | Deployment scripts and CI helpers |
| 3 | `@latimer-woods-tech/testing` | Zero-dep mock factories for all packages |
| 4 | `@latimer-woods-tech/email` | Resend integration, typed email templates |
| 4 | `@latimer-woods-tech/copy` | LLM-powered copywriting with structured output |
| 4 | `@latimer-woods-tech/content` | Content management backed by Neon |
| 4 | `@latimer-woods-tech/social` | Social post scheduling and publishing |
| 4 | `@latimer-woods-tech/seo` | Meta tag generation, OpenGraph, structured data |
| 5 | `@latimer-woods-tech/crm` | Lead tracking, conversion events, customer view |
| 5 | `@latimer-woods-tech/compliance` | TCPA/FDCPA consent and suppression management |
| 5 | `@latimer-woods-tech/admin` | Admin Hono router: users, events, health, suspend |

## Design Principles

- **No `process.env`** — every package uses Cloudflare Worker bindings (`env.VAR` / `c.env.VAR`)
- **No Node.js built-ins** — no `fs`, `path`, `crypto`, `Buffer`
- **ESM only** — no CommonJS `require()`
- **Zero `any` in public APIs** — TypeScript strict throughout
- **Explicit error handling** — no raw `fetch` without error checking
