{
  "name": "@adrper79-dot/deploy",
  "version": "0.2.0",
  "private": false,
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "scripts",
    "templates",
    "README.md",
    "CHANGELOG.md"
  ],
  "bin": {
    "factory-scaffold": "./scripts/scaffold.mjs"
  },
  "scripts": {
    "build": "echo 'No build step for deploy package'",
    "test": "echo 'No tests for deploy package'",
    "lint": "echo 'No lint step for deploy package'",
    "typecheck": "echo 'No TypeScript in deploy package'",
    "prepublish": "npm run lint && npm run typecheck && npm run test && npm run build"
  }
}
