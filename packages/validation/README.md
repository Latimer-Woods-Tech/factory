# @adrper79-dot/validation

Worker-safe validation primitives for AI output quality, synthetic monitors, and CI gates.

The package is intentionally app-agnostic: products such as SelfPrime provide their own required sections, fact checks, and brand terms while sharing the same scoring and failure contracts.
